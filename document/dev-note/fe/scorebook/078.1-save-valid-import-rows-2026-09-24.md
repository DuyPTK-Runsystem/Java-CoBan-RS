# Dev Note 078.1 FE — Lưu các dòng hợp lệ khi file có lỗi

## Trạng thái và approval

- Plan liên quan: [Plan 078 FE](../../../dev-impl-plan/fe/scorebook/078-one-column-score-import-ui-2026-09-10.md), trạng thái `APPROVED — implementation delivered with v2 bulk-extension amendment`.
- Người dùng yêu cầu cho phép lưu các dòng hợp lệ khi preview còn dòng lỗi (2026-09-24).
- Trạng thái thay đổi này: đã cập nhật UI; lint/build chưa chạy.

## Phạm vi thực tế

- Nút `Lưu` không còn bị khóa chỉ vì preview có dòng lỗi.
- Khi lưu, component gửi danh sách `items` hợp lệ backend đã trả về; dòng lỗi vẫn được hiển thị và bị bỏ qua.
- Nếu không có dòng hợp lệ, nút vẫn bị khóa và thông báo giải thích khi gọi lưu.
- Preview nêu rõ chỉ các dòng hợp lệ được lưu.

## Files thay đổi

- `FE/src/components/scorebook/BulkScoreEntryDialog.vue` — mở khóa lưu một phần và cập nhật hướng dẫn/copy.

## Quyết định triển khai

- Giữ nguyên payload và event lưu điểm hiện tại; API preview đã cung cấp riêng danh sách `items` hợp lệ.
- Plan 078 ban đầu yêu cầu khóa lưu khi có lỗi; thay đổi này theo yêu cầu mới của người dùng và là phần điều chỉnh UX sau khi file preview đã chỉ rõ dòng hợp lệ/lỗi.

## Validation Result

- `npm run lint`: NOT RUN — lệnh môi trường bị chặn do lỗi sandbox `mountinfo path is not absolute`.
- `npm run build`: NOT RUN — lệnh môi trường bị chặn do lỗi sandbox `mountinfo path is not absolute`.
- Tests: NOT RUN theo phạm vi yêu cầu.
- Browser/runtime lưu điểm: NOT RUN.

## Giới hạn

- Chưa xác minh hành vi lưu qua trình duyệt hoặc API đang chạy.
