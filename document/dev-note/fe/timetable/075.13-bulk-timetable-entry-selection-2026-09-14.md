# Dev Note 075.13 — Thêm nhiều tiết học trong một lượt

- Related Developer Plan: Plan 075 FE timetable, đã được người dùng phê duyệt trong luồng hiện tại.
- Application-document version: `v3`.

## Đã triển khai

- Luồng mới trong `TimetableEntryDialog.vue`: chọn một phân công và nhiều tiết bằng `MultiSelect`, sau đó bấm `Thêm các tiết`.
- Luồng cũ vẫn hoạt động: bấm nút `+` trên một ô sẽ mở cùng dialog và chọn sẵn ô đó; người dùng có thể giữ một tiết hoặc chọn thêm các tiết khác.
- `TimetableWorkspaceView.vue` map nhiều `periodIds` thành nhiều `upserts` trong cùng một request, dùng chung assignment, room và khoảng hiệu lực.
- Luồng sửa vẫn chọn đúng một period và giữ nút `Lưu thay đổi`; luồng xóa không thay đổi.

## Validation

- `npm run lint`: PASS.
- `npm run test`: PASS — 99 test files, 527 tests.
- `npm run build`: PASS.
- `git diff --check`: PASS.
- Browser/live API lưu batch thật: NOT RUN.

## Amendment — định mức vượt mục tiêu

- `BE/.../TimetableValidationService.java` không còn tạo issue cho `LOAD_ABOVE_TARGET`; giáo viên vượt định mức được phép, không hiển thị warning/blocking và không bị cản publish.
- `LOAD_BELOW_TARGET` vẫn tạo warning theo behavior hiện tại.
- Backend validation sau amendment: compile/bootJar đã chạy qua; full Gradle suite chưa trả kết quả cuối trong phiên (`NOT RUN` kết luận).

## Amendment — responsive conflict panel

- `FE/src/components/timetable/TimetableConflictPanel.vue` và `FE/src/styles/timetable.css` được cập nhật để header/badge tự xuống dòng, nội dung dài tự ngắt và panel không tràn ngang trên màn hình hẹp.
- Sau amendment: `npm run lint`, `npm run build`, `git diff --check`: PASS; browser screenshot verification: NOT RUN.

## Rủi ro còn lại

- Backend phải hỗ trợ nhiều `upserts` trong một mutation theo contract hiện hữu; FE không tự retry từng item nếu batch bị lỗi.
- Chưa có test component riêng cho `TimetableEntryDialog` multi-select; compile/type-check và toàn bộ test suite hiện tại PASS.

## Amendment — ADMIN/ACADEMIC_OFFICE xếp theo giáo viên

- `TimetableWorkspaceView.vue` cho phép ADMIN/ACADEMIC_OFFICE thêm tiết ở mode `Giáo viên`.
- Assignment được tải theo giáo viên đang chọn; TEACHER vẫn chỉ xem.
- `npm run lint`, `npm run build`, `git diff --check`: PASS; browser/live: NOT RUN.

## Amendment — busy/conflict wording and class-view lookup

- Busy-cell background now uses an explicit dark CSS class, suppresses `+`, and keeps the requested tooltip.
- Approved teacher unavailability is loaded for the whole revision, so adding from class view also labels affected assignments as `⚠️ Giáo viên bận`.
- Same-slot class/teacher conflicts are labeled `⛔ Lỗi chặn: Lớp đã có tiết` and `⛔ Lỗi chặn: Giáo viên đã có tiết`.
- `npm run lint`, `npm run build`, `git diff --check`: PASS; browser/live: NOT RUN.

## Amendment — busy slot presentation and assignment conflict hints

- Busy slots now use a dark background, remove the `+` action and expose the tooltip `Giáo viên đăng kí bận vào tiết này`.
- Assignment options now append an inline hint when the selected period already contains the same class or teacher; backend validation remains authoritative.
- `npm run lint`, `npm run build`, `git diff --check`: PASS; browser/live: NOT RUN.
