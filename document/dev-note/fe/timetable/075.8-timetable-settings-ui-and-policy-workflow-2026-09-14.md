# Dev Note 075.8 — Sửa Cấu hình hệ thống Thời khóa biểu

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/fe/timetable/075.8-timetable-settings-ui-and-policy-workflow-2026-09-14.md`.
- Application-document version: `v3`.
- Approval: Plan đã ghi nhận người dùng phê duyệt qua tin nhắn “oke, good” ngày 2026-09-14.

## Phạm vi thực tế

- Sửa layout minitab Cấu hình hệ thống và popup policy/miễn giảm theo viewport, loại bỏ form cứng 480px và grid 3 cột gây tràn trên màn hình hẹp.
- Policy map đúng `version`, `source`, `basePeriods`, `versionLock`; list policy gửi `page/size`.
- Tạo policy mới hỗ trợ clone từ policy active để thay đổi, không sửa trực tiếp policy active.
- Activate policy gửi `expectedVersion` để giữ optimistic lock.
- Eligibility hỗ trợ tạo, sửa `validFrom`/`validTo`/minh chứng, thu hồi có `expectedVersion`, và hiển thị status.
- Bổ sung validation teacher/date và giữ input khi API trả lỗi.

## Files thay đổi

- `FE/src/views/timetable/TimetableSettingsView.vue`
- `FE/src/services/teacherLoadApi.ts`
- `FE/src/services/teacherLoadApi.spec.ts`
- `FE/src/types/timetable.ts`
- `FE/src/styles/timetable.css`
- `document/dev-note/fe/timetable/075.8-timetable-settings-ui-and-policy-workflow-2026-09-14.md`
- `document/dev-note/fe/FE_DEV_NOTE_SUMMARY.md`
- `document/dev-note/summary/DEV_NOTE_SUMMARY.md`

## Quyết định và sai lệch

- Không thêm `SENIORITY`/miễn giảm thâm niên ở FE. v3 hiện chưa có rule code, mức giảm, điều kiện hiệu lực hoặc payload/backend evaluator cho rule này; thêm option lúc này sẽ tạo cấu hình không có ý nghĩa nghiệp vụ.
- Backend endpoint eligibility hiện trả raw array nên service giữ adapter tương thích cả array và dạng `{ result }`; không đổi backend ngoài scope Plan 075.8.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `npm run lint` | PASS |
| `npm run test` | PASS — 99 files, 527 tests; có 1 unhandled `history is not defined` từ `EnrollmentListView.spec.ts` trong quá trình teardown |
| `npm run test -- teacherLoadApi.spec.ts` | PASS — 1 file, 1 test |
| `npm run test:coverage` | FAIL — 99 files/527 tests pass nhưng Vitest báo 1 unhandled `history is not defined`; coverage tổng 85.21% statements |
| `npm run build` | PASS |
| `npm run build-storybook` | PASS — có warning có sẵn về package.json PrimeVue/eval/chunk size |
| `git diff --check` | PASS |
| Browser/Chrome live CRUD | NOT RUN — chưa thao tác dữ liệu thật thay người dùng |

## Rủi ro còn lại

- Cần browser smoke với tài khoản ADMIN hoặc ACADEMIC_OFFICE để xác nhận popup trên desktop/mobile và persistence thật.
- Cần một change request/contract v3 riêng cho miễn giảm thâm niên trước khi có thể thêm option và evaluator tương ứng.
- Unhandled `history` trong full coverage là baseline test-environment issue ngoài files của Note 075.8, cần xử lý riêng nếu muốn coverage gate sạch hoàn toàn.

## Next steps

- Xác nhận contract v3 cho `SENIORITY` ở một plan riêng.
- Chạy browser/live CRUD sau khi backend có dữ liệu test an toàn.

## Amendment 2026-09-18 — Rút gọn mô tả màn hình

- Approval: yêu cầu trực tiếp của người dùng.
- Đã xóa dòng mô tả “Quản lý chính sách định mức tiết dạy, diện miễn giảm và khung giờ học” khỏi header `Cấu hình Thời khóa biểu & Định mức`, mô tả “Các điều kiện được giảm trừ tiết dạy ngoài kiêm nhiệm chủ nhiệm (như nuôi con < 12 tháng)” khỏi tab `Xác nhận miễn giảm`, và toàn bộ minitab `Khung giờ học kỳ`; không đổi API mutation hoặc quyền truy cập.
- Khi gỡ minitab, cũng bỏ state `CALENDAR`, dữ liệu học kỳ và request `fetchSemesters` chỉ phục vụ tab này.
- File thay đổi: `FE/src/views/timetable/TimetableSettingsView.vue`.
- Validation: `npm run build` PASS; `git diff --check` PASS. `npm run lint` FAIL do hai warning có sẵn `vue/one-component-per-file` trong `FE/src/components/notification/NotificationComposer.spec.ts`, ngoài phạm vi amendment này; warning layout trong `TimetableSettingsView.vue` đã được sửa.
