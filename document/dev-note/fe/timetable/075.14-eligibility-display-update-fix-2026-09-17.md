# Dev Note 075.14 — Hiển thị và cập nhật diện miễn giảm

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/be/timetable/075.9-teacher-load-rule-extensibility-2026-09-14.md`.
- Approval: kế thừa phê duyệt Plan 075.9 qua tin nhắn “okey, impl đi”.

## Phạm vi thực tế

- Bảng chính sách hiển thị toàn bộ rule miễn giảm bổ sung của từng policy.
- Nhãn hồ sơ miễn giảm lấy từ rule cấu hình active, có fallback theo mã rule.
- Form sửa gửi `ruleCode` để thay đổi đúng diện miễn giảm.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `npm run test` | PASS — 110 files, 588 tests |
| `git diff --check` | PASS |
| Browser/live persistence | NOT RUN |

## Rủi ro còn lại

- Chưa chạy browser/live với dữ liệu policy và eligibility thực tế.
