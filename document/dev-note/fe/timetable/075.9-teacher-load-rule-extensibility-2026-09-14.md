# Dev Note 075.9 — FE rule miễn giảm mở rộng

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/be/timetable/075.9-teacher-load-rule-extensibility-2026-09-14.md`.
- Approval: người dùng phê duyệt qua tin nhắn “okey, impl đi”.

## Phạm vi thực tế

- Policy dialog hiển thị hai tham số nền và danh sách rule bổ sung có mã, tên, trigger, số tiết giảm, source.
- Clone policy giữ các rule cũ; rule mới gửi trong payload tạo policy version mới.
- Eligibility dialog lấy danh sách rule eligibility từ policy active thay vì hard-code chỉ nursing.
- Service map policy/rule typed và gọi API list/create rule.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `npm run lint` | PASS |
| `npm run test -- teacherLoadApi.spec.ts` | PASS |
| `npm run build` | PASS |
| `git diff --check` | PASS |
| Full FE coverage | NOT RUN riêng cho amendment |
| Browser/live persistence | NOT RUN |

## Rủi ro còn lại

- Cần browser smoke sau khi backend chạy migration V23 để xác nhận create policy/rule và eligibility custom trên dữ liệu thật.
