# Dev Note 075.19 — Tự tạo mã quy tắc định mức

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/be/timetable/075.9-teacher-load-rule-extensibility-2026-09-14.md`.
- Approval: Plan 075.9 đã được người dùng phê duyệt; thay đổi giao diện này được yêu cầu trực tiếp trong phiên làm việc.

## Phạm vi thực tế

- Xóa trường nhập mã quy tắc khỏi form tạo/nhân bản chính sách.
- Đổi nhãn tiết chuẩn trên form theo yêu cầu thành “Tiết chuẩn THCS”.
- Hiển thị lỗi validation và lỗi API khi lưu chính sách/diện miễn giảm bên trong popup tương ứng; lỗi tải lại sau lưu vẫn hiển thị ở trang.
- Tạo mã nội bộ từ tên diện miễn giảm khi gửi policy; chuẩn hóa tiếng Việt, giới hạn 80 ký tự và thêm hậu tố khi trùng mã trong cùng policy. Giữ nguyên mã của rule được clone.
- Sắp các trường tên, điều kiện, số tiết và căn cứ thành bốn dòng riêng; thêm nhãn và ví dụ cho từng trường cùng mô tả về cách cộng dồn mức giảm.

## Files changed

- `FE/src/views/timetable/TimetableSettingsView.vue`: xử lý tự tạo mã và cập nhật form.
- `document/dev-note/fe/timetable/075.19-autogenerated-teacher-load-rule-code-2026-09-24.md`: ghi nhận thay đổi.
- `document/dev-note/fe/FE_DEV_NOTE_SUMMARY.md`, `document/dev-note/summary/DEV_NOTE_SUMMARY.md`: cập nhật summary.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `npx eslint src/views/timetable/TimetableSettingsView.vue` | PASS |
| `npm run lint` | FAIL do 2 warning có sẵn tại `src/components/notification/NotificationComposer.spec.ts`; không có lỗi lint |
| `npm run build` | PASS (`vue-tsc --noEmit` và Vite build) |
| `git diff --check -- FE/src/views/timetable/TimetableSettingsView.vue` | PASS |
| Browser visual review tại `/v2/timetables/settings` | PASS; thấy bốn trường thành bốn dòng và không còn ô mã |
| FE tests / lưu policy trên backend thật | NOT RUN |

## Deviation và rủi ro còn lại

- Không đổi API/DTO/backend: FE tiếp tục gửi trường `ruleCode` bắt buộc, được tạo trước khi gọi API.
- Chưa kiểm chứng lưu policy thực tế với backend; không tạo dữ liệu trong phiên xác nhận UI.
