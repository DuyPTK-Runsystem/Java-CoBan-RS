# Dev Note 075.17 — Mở rộng panel cảnh báo thời khóa biểu và ẩn mã nội bộ

## Liên kết và phê duyệt

- Plan nguồn: `document/dev-impl-plan/be/timetable/075-timetable-teacher-load-2026-09-11.md`.
- Phạm vi được người dùng yêu cầu và phê duyệt trực tiếp ngày 2026-09-21.

## Phạm vi thực tế

- Cho panel trắng và danh sách cảnh báo tự mở rộng theo chiều dọc của nội dung, không giới hạn bằng scroll nội bộ.
- Giữ panel co giãn đúng chiều rộng khả dụng và ngăn flex item co/tràn ngang.
- Ẩn mã kiểm tra nội bộ như `LOAD_BELOW_TARGET` khỏi tiêu đề cảnh báo; giữ nguyên mã trong dữ liệu và nghiệp vụ.
- Thêm regression test cho layout và hiển thị mã nội bộ.

## Files thay đổi

- `FE/src/components/timetable/TimetableConflictPanel.vue`
- `FE/src/components/timetable/TimetableConflictPanel.spec.ts`
- `FE/src/styles/timetable.css`

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `npm run test -- --run src/components/timetable/TimetableConflictPanel.spec.ts` | PASS — 1 file, 1 test |
| `npm run test` | PASS — 112 files, 603 tests |
| `npm run build` | PASS |
| `npm run lint` | FAIL — 2 warning có sẵn trong `src/components/notification/NotificationComposer.spec.ts`, ngoài phạm vi |
| `git diff --check` | PASS |
| Browser/live | NOT RUN |

## Sai khác, blocker và bước tiếp theo

- Điều chỉnh theo xác nhận của người dùng: xử lý chiều dọc bằng cách bỏ `max-height` và `overflow-y-auto` khỏi danh sách cảnh báo.
- Không còn blocker thuộc phạm vi thay đổi; lint baseline vẫn có 2 warning ngoài phạm vi.
