# Dev Note 075.12 — Sửa thêm tiết TKB và hiển thị lịch bận giáo viên

- Developer Plan: Plan 075 FE, `document/dev-impl-plan/fe/timetable/075-timetable-teacher-load-ui-2026-09-11.md`; Developer Plan issue được người dùng phê duyệt bằng tin nhắn `approve`.
- Application-document version: `v3`.

## Phạm vi đã hoàn tất

- Giữ bộ lọc mặc định theo lớp đầu tiên khi mở workspace, không render toàn bộ TKB khi chưa có lớp được chọn.
- Khi chuyển sang mode `Lớp`/`Giáo viên`/`Phòng chức năng`, tự chọn option đầu tiên nếu chưa có lựa chọn; mode chưa có option không render toàn bộ entries.
- Assignment lookup lấy từ nguồn phân công `ACTIVE` theo lớp/học kỳ/khoảng hiệu lực và không loại assignment sau khi đã dùng; cùng một phân công có thể chọn cho nhiều slot khác nhau.
- Cho phép tiếp tục thêm/sửa tiết sau khi `Kiểm tra lịch` chuyển revision sang `VALIDATED`; mutation vẫn yêu cầu capability và backend version.
- Khi xem theo giáo viên, tải lịch bận `APPROVED` trong học kỳ/khoảng hiệu lực và tô xám slot bận theo ngày, buổi, tiết; hiển thị nhãn `Giáo viên bận`, không hiển thị ghi chú riêng tư.

## File thay đổi thuộc task

- `FE/src/views/timetable/TimetableWorkspaceView.vue`
- `FE/src/components/timetable/TimetableWeekGrid.vue`

## Quyết định và giới hạn

- Chỉ trạng thái `APPROVED` được dùng để tô slot bận; `PENDING`, `REJECTED`, `WITHDRAWN` bị bỏ qua.
- Lịch bận lặp theo `dayOfWeek` được hiển thị; đăng ký chỉ có `specificDate` không được suy diễn thành lịch lặp vì grid hiện tại chưa có ngày tuần đang xem.
- Không đổi backend contract/schema và không thay đổi rule conflict/publish.

## Validation

- `npm run lint`: PASS.
- `npm run test`: PASS — 99 test files, 527 tests.
- `npm run test:coverage`: PASS — 99 test files, 527 tests; tổng coverage 85.21% statements / 73.29% branches.
- `npm run build`: PASS.
- `npm run build-storybook`: PASS; có warning dependency `primevue` và chunk size từ Storybook hiện hữu.
- `git diff --check`: PASS.
- Kiểm tra lại ảnh tham chiếu: đã sửa trường hợp mode `ROOM` có placeholder nhưng grid vẫn hiển thị tất cả entries.
- Browser/live API lưu TKB thật: NOT RUN.

## Deviations và rủi ro còn lại

- Chưa thêm test view chuyên biệt cho workspace/grid; bộ test hiện tại không fail và build/type-check đã bao phủ compile contract.
- Worktree có nhiều thay đổi BE/FE/document từ các task trước; không cleanup hoặc reset các phần ngoài scope.
