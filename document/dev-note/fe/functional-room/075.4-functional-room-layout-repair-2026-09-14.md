# Dev Note 075.4 — Sửa căn chỉnh giao diện Phòng chức năng

## Liên kết và phê duyệt

- Plan liên quan: document/dev-impl-plan/be/timetable/075-timetable-teacher-load-2026-09-11.md.
- Người dùng đã duyệt phạm vi sửa giao diện Functional Room ngày 2026-09-14.
- Không thay đổi API, backend, schema hoặc quyền.

## Phạm vi thực tế

- Thay utility class kiểu Tailwind không có trong FE bằng stylesheet Functional Room được cô lập theo .functional-room-page và .functional-room-dialog.
- Căn chỉnh lại header/action, toolbar tìm kiếm-lọc, table/paginator, tràn ngang table và form/footer dialog ở desktop/mobile.
- Giữ nguyên CRUD /api/v3/functional-rooms, pagination và trạng thái hiện hữu.

## Files thay đổi

- FE/src/views/functional-room/FunctionalRoomListView.vue
- FE/src/components/functional-room/FunctionalRoomDialog.vue
- FE/src/styles/functional-room.css
- FE/src/styles.css

## Quyết định kỹ thuật

- CSS utility aliases chỉ có hiệu lực trong scope Functional Room, tránh ảnh hưởng các view đang cùng dùng tên class.
- Sửa escape selector responsive sm: thành CSS selector hợp lệ sau khi rà lại implementation.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| npm run test -- src/services/functionalRoomApi.spec.ts | PASS — 1 test |
| npm run lint | PASS |
| npm run test | PASS — 99 files, 526 tests |
| npm run build | PASS |
| npm run build-storybook | PASS; còn warning dependency/chunk-size có sẵn |
| git diff --check | PASS |
| Chrome Academic Office smoke | PASS — route /v2/functional-rooms tải được, controls/table/pagination hiển thị trong accessibility tree |
| Browser visual screenshot | NOT RUN — browser automation không trả image để xác nhận pixel-level layout |

## Sai khác, rủi ro và bước tiếp theo

- Không cần seed data để sửa căn chỉnh. Chrome hiện không có Functional Room nên chưa thể xem row populated; seed chỉ cần nếu muốn kiểm tra table có dữ liệu.
- Các thay đổi không thuộc scope 075.4 trong worktree được giữ nguyên.

## Amendment 2026-09-18

- Người dùng duyệt ẩn mô tả header, tăng khoảng cách giữa nút sửa/xóa và yêu cầu tách nút `Thêm phòng chức năng` khỏi toolbar tìm kiếm.
- Nguyên nhân khoảng cách không hiển thị: `FE/src/styles/functional-room.css` chưa được nạp vào `FunctionalRoomListView.vue`.
- Đã thêm stylesheet vào view, đặt khoảng cách desktop thực tế `2rem` giữa các khối, và giữ `1rem` trên mobile.
- Đặt trực tiếp `display: flex`, `flex-direction: column` và `gap: 1rem` cho root dialog để các trường nhập không còn dính nhau.
- Validation: `npm run build` PASS; `git diff --check` PASS. Browser visual QA PASS — trực tiếp xác nhận header-toolbar và dialog thêm phòng.
