# Dev Note 074.6 — Quay về màn hình Xếp lớp sau xác nhận

## Plan và approval

- Developer Plan: Không có plan riêng; thay đổi được thực hiện theo yêu cầu trực tiếp của người dùng ngày 2026-09-23.
- Approval: Người dùng yêu cầu sau khi xác nhận phải quay về giao diện Xếp lớp và hiện thông báo thành công thay cho trang chỉ đọc của phiên đã xong.

## Thay đổi thực tế

- Khi API xác nhận thành công, `PlacementWorkspaceView` điều hướng về route `v2-enrollments` và chuyển dấu hiệu thông báo một lần qua query.
- `EnrollmentListView` hiển thị FormAlert thành công, hướng người dùng tiếp tục xử lý các trường hợp còn lại; sau đó xóa query để không lặp thông báo khi tải lại trang.
- Lỗi xác nhận vẫn ở lại màn hình phiên và giữ xử lý lỗi hiện có.

## Files

- `FE/src/views/enrollment/PlacementWorkspaceView.vue` — điều hướng sau xác nhận thành công.
- `FE/src/views/enrollment/EnrollmentListView.vue` — nhận thông báo một lần trên màn hình Xếp lớp.

## Validation

- Test, lint, build và browser: NOT RUN theo hướng dẫn phiên làm việc.
- `git diff --check`: NOT RUN.

## Deviations và giới hạn

- Không có Developer Plan riêng cho yêu cầu chỉnh UX này.
- Chưa có bằng chứng runtime/browser; các kiểm tra validation không được chạy.
