# Dev Note 074.4 — Làm rõ phiên xếp lớp nháp chưa có kết quả

## Liên kết và approval

- Developer Plan: `document/dev-impl-plan/fe/enrollment/074.4-placement-draft-empty-state-2026-09-23.md`.
- Version: `v3`.
- Plan được người dùng phê duyệt qua chỉ đạo tiếp tục triển khai ngày `2026-09-23`.

## Phạm vi thực tế

- Với session `DRAFT` đã tải xong nhưng chưa có kết quả, màn hình hiện thông báo `Phiên nháp chưa có kết quả mô phỏng. Chọn “Mô phỏng” để xem lớp đề xuất.`
- Nút đổi thành `Mô phỏng` cho lần chạy đầu; các trạng thái khác vẫn ghi `Mô phỏng lại`.
- Icon lần đầu là `pi pi-play`; icon chạy lại vẫn là `pi pi-refresh`.
- Empty state không xuất hiện khi đang tải; event, disabled state và loading behavior của nút giữ nguyên.
- Không thay đổi API, backend, business rules hoặc dữ liệu runtime.

## Files đã thay đổi

- Production FE: `FE/src/components/enrollment/PlacementWorkspaceReview.vue`.
- Test: `FE/src/components/enrollment/PlacementWorkspaceReview.spec.ts` (TEST culi độc lập với DEV).
- Kế hoạch và Dev Note: Plan 074.4, note này, `document/dev-note/fe/FE_DEV_NOTE_SUMMARY.md`, `document/dev-note/summary/DEV_NOTE_SUMMARY.md`.

## Validation

- `cd FE && npm run test -- src/components/enrollment/PlacementWorkspaceReview.spec.ts`: `PASS`, 9/9.
- `cd FE && npm run lint`: `FAIL` do hai warning `vue/one-component-per-file` có sẵn trong `src/components/notification/NotificationComposer.spec.ts:8,14`; không có lỗi lint mới.
- `cd FE && npm run build`: `PASS` (`vue-tsc` và Vite build).
- `git diff --check`: `PASS`.
- FE full test/coverage/Storybook và browser/live: `NOT RUN`.

## Sai lệch, giới hạn và bước tiếp theo

- Không có sai lệch phạm vi.
- Chưa capture Network response của phiên người dùng; thay đổi này làm rõ trạng thái `DRAFT` có danh sách kết quả rỗng, không chứng minh một response runtime khác không thể chứa `data: null`.
- Nếu Network vẫn cho thấy literal `data: null`, cần điều tra theo request/response capture tương ứng.
