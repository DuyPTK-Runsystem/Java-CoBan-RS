# Dev Note 074.3 — Placement review copy, student name và overflow thủ công

## Liên kết và approval

- Developer Plan: `document/dev-impl-plan/fe/enrollment/074.3-placement-review-copy-student-name-capacity-recovery-2026-09-22.md`.
- Version: `v3`.
- Plan và amendment được người dùng phê duyệt qua agent.

## Phạm vi thực tế

- Xóa hai eyebrow `XẾP LỚP TỰ ĐỘNG` màu tím.
- Xóa hai câu hướng dẫn về vượt sĩ số và dữ liệu thiếu/bằng điểm.
- Sửa cache tên học sinh dùng contract `studentName` thay vì field không tồn tại `fullName`.
- Giữ capacity runtime `7A1 = 7` theo chủ đích kiểm thử giới hạn.
- Đổi overflow `CAPACITY_EXCEEDED` thành `MANUAL_REQUIRED + WARNING`; phần `AUTO_ASSIGNED` vẫn có thể xác nhận và không vượt capacity.
- `SCORE_TIE`/overflow tiếp tục không tạo enrollment tự động.

## Files thay đổi

- `FE/src/components/enrollment/PlacementSessionSetup.vue`
- `FE/src/components/enrollment/PlacementWorkspaceReview.vue`
- `FE/src/views/enrollment/PlacementWorkspaceView.vue`
- `FE/src/components/enrollment/PlacementSessionSetup.spec.ts`
- `FE/src/components/enrollment/PlacementWorkspaceReview.spec.ts`
- `FE/src/views/enrollment/PlacementWorkspaceView.spec.ts`
- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/placement/service/support/PlacementAllocationEngine.java`
- `BE/BaiTap-RS/src/test/java/com/JavaTraining/BaiTap_RS/placement/service/PlacementServiceTest.java`
- `BE/BaiTap-RS/src/test/java/com/JavaTraining/BaiTap_RS/placement/service/support/PlacementAllocationEngineTest.java`

## Validation

- Backend focused placement tests: `PASS`; `PlacementServiceTest` và `PlacementAllocationEngineTest`; Gradle `BUILD SUCCESSFUL`.
- FE affected specs: `PASS`; 3 files, 21 tests.
- FE production build: `PASS`.
- PMD: `NOT RUN` theo chỉ định trực tiếp của người dùng.
- Checkstyle: `NOT RUN` theo chỉ định trực tiếp của người dùng.
- Full backend test/build, FE lint/coverage/Storybook: `NOT RUN` để giới hạn thời gian/token theo yêu cầu.
- Browser live: tên thật hiển thị; ba copy đã biến mất; phiên #13 với capacity `7/10/10/10` ở trạng thái `READY_FOR_CONFIRM`; overflow và tie hiển thị `Cần xếp thủ công / Cảnh báo`; nút xác nhận được bật.
- Browser confirm persistence: `NOT VERIFIED`; modal được mở nhưng chưa có bằng chứng request/persistence thành công.

## Sai lệch và rủi ro còn lại

- Không khôi phục capacity 7A1 lên 10; amendment giữ capacity thấp để kiểm thử boundary.
- Không thay đổi API, schema, seed hoặc global CSS.
- Cần kiểm tra riêng persistence sau confirm nếu muốn đóng bằng chứng E2E enrollment.


## Căn chỉnh trạng thái học sinh — 2026-09-23

- Đưa dòng đủ điều kiện lên lớp vào cột ngay bên phải `Nguồn học sinh` trên cùng hàng.
- Candidate `CONTINUING` không mở hàng chi tiết rỗng; `NEW_ADMISSION`/`REPEAT` vẫn mở biểu mẫu bổ sung.
- File sửa: `FE/src/components/enrollment/PlacementSessionSetup.vue`.
- Focused ESLint: `PASS`; `npm run build`: `PASS`; `git diff --check`: `PASS`.
- Full `npm run lint`: `FAIL` do hai warning `vue/one-component-per-file` có sẵn trong `NotificationComposer.spec.ts`. Tests và browser QA: `NOT RUN`.


## Bố cục responsive — 2026-09-23

- Ở màn hình rộng tối đa `640px`, bảng ứng viên chuyển thành thẻ dọc; nhãn tên và nguồn được hiển thị cùng tên học sinh, nguồn và trạng thái, không cần kéo ngang bảng.
- Chrome kiểm tra ở `390 × 844` cho thấy các trường và dòng trạng thái nằm gọn trong thẻ; đã trả viewport Chrome về kích thước mặc định.
- Focused ESLint, `npm run build`, `git diff --check`: `PASS`. Full lint: `FAIL` do hai warning có sẵn tại `NotificationComposer.spec.ts`. Tests: `NOT RUN`.
