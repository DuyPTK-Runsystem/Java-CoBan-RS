# 079.4 BE — Policy GET semantics and effective-date validation

## Related plan

- Developer Plan: [Plan 079 BE — Sổ đầu bài](../../../dev-impl-plan/be/lesson-log/079-lesson-log-lifecycle-and-audit-2026-09-14.md)
- Approval: Plan 079 implementation was approved; user approved this scoped fix on 2026-09-24.

## Actual scope

- `GET /api/v3/lesson-logs/policy` without a date now returns the latest policy version for settings. A supplied date still resolves the policy effective on that date.
- Policy creation rejects an effective date that is not after today in `Asia/Ho_Chi_Minh` or not after the latest policy's effective date. Database uniqueness constraints remain unchanged.

## Files changed

- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/lessonlog/service/LessonLogPolicyService.java` — latest-version GET behavior and latest effective date passed into validation.
- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/lessonlog/service/LessonLogPolicyValidator.java` — strict future and monotonic effective-date checks with clear 422 messages.

## Validation

- `./gradlew --no-daemon --max-workers=1 compileJava`: PASS.
- `./gradlew --no-daemon --max-workers=1 checkstyleMain pmdMain compileJava`: FAIL; repo Checkstyle reported 977 warnings across 121 files, including existing import-order/line-length warnings in lesson-log files; PMD reported 7 `DemoIdentitySeeder` violations. No PMD finding matched the changed lesson-log files.
- Backend tests: NOT RUN, as agreed in the approved plan.
- Backend `build`: NOT RUN; the full build would include the unrequested test task.
- `git diff --check`: PASS.
- Runtime: Spring Boot DevTools logged a restart at 15:40:32; the existing settings page loaded after refresh without the error banner. The exact HTTP response was not captured; no policy or other database row was written.

## Review and remaining notes

- Independent source review found no issue in the scoped patch.
- Competing inserts still rely on existing unique constraints to reject duplicate versions; explicit policy-row serialization was not changed in this scoped fix.
- No deviation from the approved scope.
