# Dev Note 076.5 BE — Embedded idempotency repository query

## Developer Plan và approval

- Related plan: `document/dev-impl-plan/be/notification/076.3-notification-ux-audience-lookup-2026-09-17.md` (preserves the notification idempotency flow).
- Approval: user directly authorized the minimal v3 notification patch in the agent message on 2026-09-17.

## Actual scope

- Corrected the repository lookup used by `POST /api/v3/notifications` when the client supplies an idempotency key.
- Added a Spring/H2 regression test that persists and retrieves the embedded idempotency key.
- No route, DTO, authorization, lifecycle, migration, or v2 contract changed.

## Files changed

- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/notification/repository/NotificationRepository.java`
- `BE/BaiTap-RS/src/test/java/com/JavaTraining/BaiTap_RS/notification/NotificationRepositoryTest.java`

## Validation Result

- Focused test and JaCoCo: **PASS** — `GRADLE_USER_HOME=/tmp/java-coban-root-validation ./gradlew cleanTest test --tests 'com.JavaTraining.BaiTap_RS.notification.NotificationRepositoryTest' --no-daemon --max-workers=1 --console=plain`.
- Full test: **FAIL** — `GRADLE_USER_HOME=/tmp/java-coban-root-validation ./gradlew test --no-daemon --max-workers=1 --console=plain`; 442 tests completed, 8 failed after `OutOfMemoryError`/Java heap-space failures in unrelated integration contexts.
- Checkstyle: **PASS** — `GRADLE_USER_HOME=/tmp/java-coban-root-validation ./gradlew checkstyleMain --no-daemon --max-workers=1 --console=plain`.
- PMD: **FAIL** — `GRADLE_USER_HOME=/tmp/java-coban-root-validation ./gradlew pmdMain --no-daemon --max-workers=1 --console=plain`; 9 existing `lessonlog` violations, outside this patch.
- Build: **FAIL** — `GRADLE_USER_HOME=/tmp/java-coban-root-validation ./gradlew build --no-daemon --max-workers=1 --console=plain`, blocked by the same PMD baseline violations.

## Deviation and remaining risk

- Debug round 1 fixed the test fixture to assert the entity returned by `saveAndFlush`; the production patch was unchanged.
- Existing summary files contained user WIP; only notification summary rows were updated to record this patch and its validation boundary.
