# Dev Note 73.0 — v3 TBD-001 Teacher Load Policy

## Liên kết

- Related Developer Plan: `document/dev-impl-plan/summary/MASTER_PLAN_V3-2026-09-09.md` và
  `document/dev-impl-plan/be/073-v3-foundation-contract-2026-09-09.md`
- Approval: người dùng đã approval Developer Plan qua agent message ngày 2026-09-10.

## Phạm vi đã thực hiện

Ghi nhận nội dung nghiệp vụ của `TBD-001` cho định mức tiết dạy giáo viên THCS:

- Chuẩn: `19` tiết/tuần.
- GVCN: giảm `4` tiết/tuần.
- GV nữ nuôi con dưới `12` tháng tuổi: giảm thêm `3` tiết/tuần.
- Các mức giảm được cộng dồn; trường hợp đồng thời là GVCN và thuộc diện nuôi con nhỏ
  còn `12` tiết/tuần.

Quy tắc được định hướng biểu diễn qua tham số của `TeacherLoadPolicy`, không hard-code
trong timetable engine.

## Files changed

- v3 README, `ApplicationContext` và `RequirementBaseline`: cập nhật trạng thái
  `TBD-001` và nguyên tắc policy.
- v3 Timetable/Teacher Load module: ghi công thức, tham số và invariant policy.
- `CR-V3-001`, Master Plan v3 và `specification.html`: đồng bộ contract checkpoint và
  decision table.

## Quyết định quan trọng

Nội dung rule đã được cung cấp ở mức nghiệp vụ nhưng `source`, `effectiveFrom` và
`version` vẫn chưa có. Vì vậy `TBD-001` được ghi là `PARTIALLY RESOLVED`, chưa được coi
là policy pháp lý active.

## Validation

- `git diff --check`: PASS.
- `rg -n -C 1 'TBD-001|19.*tiết|giảm.*4|giảm thêm.*3|PARTIALLY RESOLVED|source.*effective' ...`:
  PASS; các tài liệu liên quan đã phản ánh cùng rule và trạng thái còn thiếu metadata.
- Backend tests, FE tests, build, browser/live: NOT RUN; thay đổi chỉ là tài liệu.

## Deviations

Không có deviation so với Developer Plan. Dev Note dùng sequence `73.0` theo yêu cầu
của người dùng.

## Blockers và next steps

- Bổ sung văn bản/source, ngày hiệu lực và phiên bản policy để đóng hoàn toàn `TBD-001`.
- Chưa triển khai code/schema hoặc Plan 075; việc này cần contract checkpoint và approval
  tương ứng.
