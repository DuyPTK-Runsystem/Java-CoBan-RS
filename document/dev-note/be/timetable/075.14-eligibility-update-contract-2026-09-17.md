# Dev Note 075.14 — Mở rộng contract cập nhật diện miễn giảm

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/be/timetable/075.9-teacher-load-rule-extensibility-2026-09-14.md`.
- Approval: kế thừa phê duyệt Plan 075.9 qua tin nhắn “okey, impl đi”.

## Phạm vi thực tế

- Thêm `ruleCode` tùy chọn vào `ReqUpdateEligibilityDTO`.
- Service cập nhật mã rule cùng các trường hiệu lực, minh chứng và trạng thái.
- Không thay đổi migration; cột `rule_code` đã tồn tại trong contract hiện hành.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| `./gradlew compileJava` | PASS |
| `./gradlew pmdMain` | PASS |
| `./gradlew checkstyleMain` | PASS theo exit code; còn warning baseline |
| `./gradlew test` | FAIL — 449 tests, 15 integration context failures do Java heap/OOM |
| `./gradlew build -x test -x pmdTest` | PASS |
| `pmdTest` | FAIL — 206 vi phạm test baseline |
| MySQL/Flyway/browser/live | NOT RUN |

## Rủi ro còn lại

- Full backend quality gate chưa xanh do OOM integration test và PMD test baseline ngoài phạm vi.
