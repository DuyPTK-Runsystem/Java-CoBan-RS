# Dev Note 075.9 — Mở rộng rule miễn giảm định mức

## Liên kết và phê duyệt

- Plan: `document/dev-impl-plan/be/timetable/075.9-teacher-load-rule-extensibility-2026-09-14.md`.
- Application-document version: `v3`; scope theo Plan 075 và Thông tư 05/2025/TT-BGDĐT.
- Approval: người dùng phê duyệt qua tin nhắn “okey, impl đi”.

## Phạm vi thực tế

- Giữ `basePeriods`, `homeroomReduction`, `nursingReduction` tương thích dữ liệu cũ.
- Thêm `teacher_load_rule` theo từng policy, seed `HOMEROOM` và `NURSING_CHILD_UNDER_12M` cho policy hiện hữu.
- Policy mới clone rule hiện tại và nhận rule custom; policy active vẫn bất biến.
- Evaluator tự nhận diện GVCN từ `HomeroomAssignment` active, eligibility theo `ruleCode`, rồi cộng tất cả reduction; không có trần tổng mức giảm.
- Thêm API list/create rule dưới policy, chặn thêm rule trực tiếp vào policy active.

## Validation Result

| Kiểm tra | Kết quả |
| --- | --- |
| Focused evaluator test | PASS — GVCN + nursing regression và custom rule cộng dồn |
| `./gradlew pmdMain` | PASS |
| `./gradlew checkstyleMain` | PASS theo exit code; còn warning baseline ngoài amendment |
| `./gradlew test` | FAIL — 441/442 pass; `InvalidJwtIntegrationTest` baseline context failure |
| `npm run lint` | PASS |
| `npm run test -- teacherLoadApi.spec.ts` | PASS |
| `npm run build` | PASS |
| `git diff --check` | PASS |
| Browser/live persistence | NOT RUN |

## Rủi ro còn lại

- `pmdTest` còn fail bởi các vi phạm test baseline ngoài scope.
- Chưa chạy browser với backend đã migrate và chưa tạo dữ liệu rule thật.
