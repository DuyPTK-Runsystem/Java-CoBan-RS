# Dev Note 075.11 — Baseline định mức giáo viên mặc định

## Liên kết và phê duyệt

- Scope: sửa hành vi evaluator theo yêu cầu nghiệp vụ Plan 075.
- Application-document version: `v3`.
- Người dùng đã yêu cầu trực tiếp sửa lỗi `LOAD_UNDETERMINED` trong phiên ngày 2026-09-14.

## Phạm vi thực tế

- Giữ baseline mặc định `19 tiết/tuần` khi chưa có `TeacherLoadPolicy` bổ sung.
- Giáo viên có phân công chủ nhiệm đang hiệu lực mặc định được giảm `4 tiết`.
- Không còn trả `LOAD_UNDETERMINED` chỉ vì chưa có policy bổ sung active.
- Không áp dụng mặc định `NURSING_CHILD_UNDER_12M`; rule/eligibility bổ sung chỉ được tính khi policy active có cấu hình tương ứng.
- Giữ nguyên DTO, API và trạng thái response; policy active vẫn được dùng cho các rule bổ sung do Giáo vụ/ADMIN cấu hình.

## Files thay đổi

- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/timetable/service/TeacherLoadEvaluator.java`
- `BE/BaiTap-RS/src/test/java/com/JavaTraining/BaiTap_RS/timetable/service/TeacherLoadEvaluatorTest.java`
- `document/dev-note/be/timetable/075.11-baseline-teacher-load-2026-09-14.md`
- `document/dev-note/be/BE_DEV_NOTE_SUMMARY.md`
- `document/dev-note/summary/DEV_NOTE_SUMMARY.md`

## Validation Result

| Check | Result | Evidence |
|---|---|---|
| Focused evaluator test | PASS | `./gradlew test --tests 'com.JavaTraining.BaiTap_RS.timetable.service.TeacherLoadEvaluatorTest'` |
| `test` full suite | FAIL | 443 tests, 1 `ExpiredJwtIntegrationTest` failure with `OutOfMemoryError`; focused suite passes |
| `checkstyleMain` | PASS with baseline warnings | Task exit successful; existing warnings remain, including unrelated files |
| `pmdMain` | PASS | Task exit successful; misconfigured `LoosePackageCoupling` was reported by PMD but not changed |
| `build` | FAIL | Full test failure and 124 baseline `pmdTest` violations |
| `git diff --check` | PASS | No whitespace error |
| Browser/live QA | NOT RUN | Chưa chạy live backend/browser sau thay đổi |

## Sai khác và rủi ro còn lại

- Full quality gate chưa xanh do lỗi test tích hợp `OutOfMemoryError` và PMD test baseline ngoài scope.
- Cần reload/restart backend đang chạy để Chrome nhận code evaluator mới.
- Chưa xác nhận dữ liệu live sau restart; không claim lỗi trên môi trường live đã biến mất.

