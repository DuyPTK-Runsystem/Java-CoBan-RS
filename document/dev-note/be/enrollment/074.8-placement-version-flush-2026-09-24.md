# Plan 074.8 — Flush version trước response placement

## Liên kết và approval

- Developer Plan liên quan: [Plan 074 BE](../../../dev-impl-plan/be/enrollment/074-rule-based-class-placement-2026-09-10.md), đã được duyệt; trạng thái tổng thể của Plan 074 vẫn là implemented slice, validation incomplete.
- Phạm vi: sửa lỗi optimistic version khi thao tác mô phỏng/cập nhật/xác nhận/hủy phiên trả DTO trước khi transaction flush.

## Thay đổi thực tế

- `PlacementSimulationSupport.simulate` dùng `saveAndFlush` trước khi dựng response; Hibernate tăng trường `@Version` trước khi DTO trả version về FE.
- `PlacementService.update`, `confirm`, `cancel` cũng flush trước response để FE không giữ version cũ sau các thao tác thay đổi session.
- Không đổi FE contract; FE tiếp tục gửi `session.version` hiện hành làm `expectedVersion`.

## Validation Result

- `./gradlew compileJava --offline --no-daemon --max-workers=1 -g /home/duyptk/.gradle`: **PASS** (`BUILD SUCCESSFUL`).
- Backend tests, Checkstyle, PMD, full build, Chrome post-fix và `git diff --check`: **NOT RUN**.

## Deviations và rủi ro còn lại

- Không thêm hoặc chạy tests trong lượt này.
- Chrome phiên #9 hiện ghi snapshot 7A1 có 7 chỗ, trong khi danh sách lớp hiện tại ghi 10; sau khi khắc phục version, phiên #9 vẫn cần tạo lại để cập nhật snapshot capacity.
- Cần xác minh thao tác mô phỏng rồi xác nhận phiên mới trên Chrome để chứng minh expectedVersion sau response khớp database.
