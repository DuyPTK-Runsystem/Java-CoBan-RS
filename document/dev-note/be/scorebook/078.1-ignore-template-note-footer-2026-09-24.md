# Dev Note 078.1 BE — Bỏ qua phần hướng dẫn cuối file khi preview

## Trạng thái và approval

- Plan liên quan: [Plan 078 BE](../../../dev-impl-plan/be/scorebook/078-one-column-score-import-2026-09-10.md), trạng thái `APPROVED — implementation delivered with v2 bulk-extension amendment`.
- Yêu cầu tiếp nối được người dùng xác nhận: bỏ qua các dòng hướng dẫn `Ghi chú:` cuối file khi gọi `/preview` (2026-09-24).
- Thay đổi mã đã áp dụng; quality validation chưa hoàn tất.

## Phạm vi thực tế

- `/preview` dừng duyệt khi ô cột A đúng nhãn `Ghi chú:` của footer template. Footer và các dòng hướng dẫn sau đó không được tính là dữ liệu, lỗi, hay item preview.
- Cột `Ghi chú` của từng dòng học sinh vẫn được đọc và kiểm tra như trước.

## Files thay đổi

- `BE/BaiTap-RS/src/main/java/com/JavaTraining/BaiTap_RS/scorebook/service/BulkScoreFileService.java` — thêm marker footer và ngắt vòng lặp preview trước khi parse dòng đó.

## Quyết định triển khai

- Dùng nhãn do `createImportNotes` tạo ở cột A làm ranh giới giữa roster và phần hướng dẫn. Dòng trống trước nhãn vẫn được bỏ qua.
- Không thay đổi payload score, validation roster, duplicate, semantics của note, hoặc luồng bulk write.
- Unit-test plan của Plan 078 đã được duyệt; test không chạy trong lượt này.

## Validation Result

- `test`: NOT RUN — không chạy test trong lượt này.
- `checkstyle`: PASS — `./gradlew --offline checkstyleMain --no-daemon --max-workers=1 --console=plain`; có 977 warning trên 121 file, không có entry cho `BulkScoreFileService`.
- `PMD`: FAIL — `./gradlew --offline checkstyleMain pmdMain compileJava --no-daemon --max-workers=1`; 7 violation `FieldDeclarationsShouldBeAtStartOfClass` ở `DemoIdentitySeeder.java:156,179-184`, không thuộc file thay đổi.
- `build`: NOT RUN — full build không chạy.
- Compile: PASS — `./gradlew --offline compileJava --rerun-tasks --no-daemon --max-workers=1`; 21 deprecation warnings ở batch package.
- `git diff --check` cho service và hai summary: PASS.
- Static source inspection: marker guard đứng trước `isBlankRow`/`parseRow`; `parseRow` vẫn đọc cell Ghi chú của học sinh.
- Debug iterations: 0.

## Giới hạn và bước tiếp theo

- Cần regression test cho template có footer và test backend liên quan trước khi xác nhận toàn bộ quality gates.
