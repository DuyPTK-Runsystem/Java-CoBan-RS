# Dev Note 73.1 — v3 TBD-005 Score Import Contract

## Liên kết

- Related Developer Plan: Plan 073 amendment trong `document/dev-impl-plan/summary/MASTER_PLAN_V3-2026-09-09.md`.
- Approval: người dùng đã approval amendment qua agent message ngày 2026-09-10.

## Phạm vi đã thực hiện

Ghi nhận các quyết định nghiệp vụ cho `TBD-005`:

- Import dùng file `.xlsx`.
- Preview là bắt buộc trước khi xác nhận thao tác.
- Mỗi lần upload chỉ xử lý một `assessmentColumnId`, không multi-column.
- Cho phép update ô đã có điểm sau khi user review giá trị cũ, giá trị mới và thông tin học sinh.
- Quy ước giá trị: `0–10` là `SCORED`, `11` là `ABSENT`, `12` là `EXEMPTED`.

## Files changed

- v3 README, `RequirementBaseline`, module Query/Score Import và `CR-V3-001`.
- Master Plan v3 và `specification.html`.

## Quyết định còn mở

Plan 078 vẫn phải chốt template/header mapping, khóa định danh chính thức, rounding/status
mapping đầy đủ, row error model, size limits, blank-cell semantics, cách xử lý `CANCELLED`
và partial-commit policy. Không tự suy diễn các mục này từ nội dung hiện tại.

## Validation

- `git diff --check`: PASS.
- Search consistency cho `TBD-005`, `.xlsx`, `assessmentColumnId`, preview và mã `11/12`:
  PASS.
- Backend tests, FE tests, build, browser/live: NOT RUN; thay đổi chỉ là tài liệu.

## Deviations và next steps

- Không có deviation so với amendment được approval.
- Plan 078 cần chuyển các quyết định trên thành template, contract DTO/API, fixture và
  validation/preview test cases trước implementation.
