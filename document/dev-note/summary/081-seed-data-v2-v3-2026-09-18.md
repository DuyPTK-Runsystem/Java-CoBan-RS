# Dev Note 081 — Bộ dữ liệu seed v2 và CR v3

- Developer Plan: [`document/dev-impl-plan/summary/081-seed-data-v2-v3-2026-09-18.md`](../../dev-impl-plan/summary/081-seed-data-v2-v3-2026-09-18.md), đã được người dùng phê duyệt.
- Scope triển khai thực tế: baseline v2, placement, notification, functional-room, scorebook và Plan 081 mở rộng. TKB production seed phủ 16 lớp `6A1`–`9A4`, HK1/HK2 với 376/388 tiết môn học, revision `DRAFT`, `day_of_week=1..5`, marker `SEED_PLAN_081_FULL_V2`. 40 học sinh `STU2600041`–`STU2600080` vẫn `ACTIVE` nhưng không tạo enrollment mới để phục vụ demo unassigned; không set nullable class và không xóa enrollment cũ. Bổ sung 2 phòng Tin (`TIN-1`, `TIN-2`), 1 phòng Nghề (`NGHE-1`) và mapping subject-functional-room idempotent. Bổ sung historical academic seed `2025-2026` `CLOSED` với 4 lớp Khối 6, 40 enrollment `COMPLETED`, scorebook `80`, columns `320`, scores `3200`, annual/term transcript `40/80` và subject results `400/800`; điểm `6.5..9.5`.
- Row data chi tiết đã tách khỏi Plan 081 thành thư mục [081-seed-data-v2-v3-2026-09-18](081-seed-data-v2-v3-2026-09-18/), gồm các file seed theo nhóm và fixture chưa seed.
- Files changed: `DemoAcademicCatalogSeeder.java`, `DemoIdentitySeeder.java`, `DemoPlacementSeeder.java`, `DemoPlacementSeedWriter.java`, `DemoNotificationSeeder.java`, `DemoFunctionalRoomSeeder.java`, `DemoScorebookSeeder.java`, `DemoTimetableSeeder.java`, `DemoDataSeeder.java`, `DemoDataSeederIntegrationTest.java`, scope fixture docs, Developer Plan 081 và Dev Note này.
- Quyết định chính: seed không xóa dữ liệu; placement dùng `scopeSnapshot.seedKey` làm natural key; notification dùng embedded `idempotency_key`; full timetable dùng audit marker version mới và chỉ nâng cấp revision G7 cũ khi toàn bộ entry cũ có marker legacy, còn dữ liệu ngoài fixture thì no-op; các scenario score-import, lesson log và policy `TBD` chưa seed vì contract/schema hoặc FK fixture chưa đủ an toàn.
- Validation (TEST culi cuối, 2026-09-22): focused historical test PASS trong `1m34s` sau khi bổ sung assertion; `compileJava` PASS; `checkstyleMain` PASS với `978` warning baseline; `pmdMain` FAIL với 7 vi phạm baseline tại `DemoIdentitySeeder.java`; `git diff --check` PASS; full `test` FAIL/timeout sau `120s` với nhiều lỗi context tại `DemoIdentitySeeder.java:288`; full `build` NOT RUN vì full test không hoàn tất; objective-focused evidence vẫn PASS, không claim backend full green.
- Deviations: yêu cầu mới đã mở rộng từ slice G7 sang full timetable 16 lớp và đổi G7 thành unassigned. Luna DEV không sửa test; TEST culi đã cập nhật assertion và kiểm chứng độc lập. Không triển khai các nhóm còn TBD/chưa có bảng import.
- Blockers/risks: full backend gate còn `554 tests/99 failures` liên quan `Missing seeded role: ACADEMIC_OFFICE` tại `DemoIdentitySeeder.java:185` và `313` `pmdTest` baseline violations; database đã có fixture cũ sẽ giữ dữ liệu cũ theo nguyên tắc non-destructive.
- Quy mô đã chốt lại: 4 khối (6–9), mỗi khối 4 lớp, mỗi lớp 10 học sinh; tổng 160 học sinh; mỗi giáo viên tối đa 2 môn.
- Bổ sung phân công giáo viên theo từng lớp và môn; timetable dùng toàn bộ 32 bảng lớp–học kỳ trong tài liệu thiết kế, không thêm workflow state ngoài `DRAFT`.
- Bổ sung bảng học sinh canonical đủ 160 rows, mã `STU2600001`–`STU2600160`, phân đều 10 học sinh cho mỗi lớp.
- Dọn dữ liệu trùng trong mục A.3: giữ bảng lớp 16 lớp và bảng học sinh canonical duy nhất; username học sinh có hậu tố mã lớp để luôn unique.
- Điều chỉnh username học sinh theo giới hạn schema 20 ký tự: hậu tố lớp compact (`6A1` → `61`) nối trực tiếp, vẫn unique và không vượt `app_user.user_name`.
- Bổ sung 4 phòng chức năng, scorebook Toán 6A1 HK1 với 4 cột và 40 score rows; trạng thái non-SCORED giữ `scoreValue = null` theo constraint hiện tại.
- Sửa V27 để Flyway H2 chạy được bằng cách tách từng `ADD COLUMN` và `ADD CONSTRAINT`; không thay đổi semantics production.
- Điều chỉnh score demo về range hợp lệ `5–10`, đồng bộ username compact và notification receipts theo giới hạn `app_user.user_name` 20 ký tự.
- Không seed timetable scenarios ngoài G7, lesson log phụ thuộc timetable, score-import session chưa có bảng/contract, teacher-load policy `TBD`, transcript/average/retake và attendance mở rộng.
- Historical transcript chỉ chứng minh score/học bạ chính thức ở trạng thái `FINISH` và version khớp; không có rule/record `PROMOTED` chính thức trong v3 nên không claim “đủ lên lớp” theo nghĩa nghiệp vụ.
- Full production write-set đã triển khai: TKB đủ 16 lớp, HK1/HK2 `376/388` tiết, ngày `1..5`; G7 `40` unassigned/`120` enrollment. DB sạch dùng canonical rooms `LAB-PHY-01`, `LAB-CHEM-01`, `LAB-BIO-01`, `TIN-1`, `TIN-2`, `NGHE-1`; `LAB-IT-01` không còn seed mới. Mapping môn–phòng, marker `SEED_PLAN_081_FULL_V2` và idempotency đã được ghi nhận; DB có fixture cũ không bị xóa.
- Refactor PMD production: tách schedule catalog và hai gateway repository; không đổi 376/388 slot, room binding, marker, ngày 1..5 hoặc idempotency. PMD/test chưa chạy trong phiên Luna DEV.
- Identity fixture hiện có đúng 160 tên unique, gồm 100 tên họ Nguyễn và 60 tên họ Lý; semantics unassigned năm `2026-2027` được bảo toàn.
