# Dev Note 081 — Bộ dữ liệu seed v2 và CR v3

- Developer Plan: [`document/dev-impl-plan/summary/081-seed-data-v2-v3-2026-09-18.md`](../../dev-impl-plan/summary/081-seed-data-v2-v3-2026-09-18.md), đã được người dùng phê duyệt.
- Scope hoàn thành: triển khai seed bật bằng `app.seed.demo.enabled=true` cho baseline v2, placement, notification, functional-room và scorebook fixture; bổ sung test integration cho counts, trạng thái và idempotency. Teacher/student fixture dùng mã canonical theo Plan 081.
- Files changed: `DemoAcademicCatalogSeeder.java`, `DemoIdentitySeeder.java`, `DemoPlacementSeeder.java`, `DemoPlacementSeedWriter.java`, `DemoNotificationSeeder.java`, `DemoFunctionalRoomSeeder.java`, `DemoScorebookSeeder.java`, `DemoDataSeederIntegrationTest.java`, Developer Plan 081 và Dev Note này.
- Quyết định chính: seed không xóa dữ liệu; placement dùng `scopeSnapshot.seedKey` làm natural key; notification dùng embedded `idempotency_key`; score-import, timetable entries, lesson log và policy `TBD` chưa seed vì contract/schema hoặc FK fixture chưa đủ an toàn.
- Validation: `git diff --check` — PASS; `compileJava compileTestJava` — PASS; `NotificationMigrationTest` — PASS sau khi tách các câu ALTER của V27; `DemoDataSeederIntegrationTest` — PASS 5/5; `checkstyleMain` — exit 0 với 969 warning nền; full `test` — 463 declared, 462 completed, 1 skipped, 7 failed do Java heap/context nền ở `StudentControllerIntegrationTest`; `pmdMain` — FAIL 1 violation baseline tại `LessonLogScheduleEntryFactory`; full `build` chưa xanh do các gate nền.
- Deviations: Plan 081 ban đầu là đặc tả dữ liệu; yêu cầu mới đã phê duyệt triển khai một slice có contract thực tế. Không triển khai các nhóm còn TBD/chưa có bảng import.
- Blockers/risks: full backend gate còn Java heap/context failure ở `StudentControllerIntegrationTest` và 1 PMD baseline ở `LessonLogScheduleEntryFactory`; database đã có fixture cũ sẽ giữ dữ liệu cũ theo nguyên tắc non-destructive.
- Quy mô đã chốt lại: 4 khối (6–9), mỗi khối 4 lớp, mỗi lớp 10 học sinh; tổng 160 học sinh; mỗi giáo viên tối đa 2 môn.
- Bổ sung phân công giáo viên theo từng lớp và môn; loại timetable khỏi phạm vi rows seed để người dùng tự xếp.
- Bổ sung bảng học sinh canonical đủ 160 rows, mã `STU2600001`–`STU2600160`, phân đều 10 học sinh cho mỗi lớp.
- Dọn dữ liệu trùng trong mục A.3: giữ bảng lớp 16 lớp và bảng học sinh canonical duy nhất; username học sinh có hậu tố mã lớp để luôn unique.
- Điều chỉnh username học sinh theo giới hạn schema 20 ký tự: hậu tố lớp compact (`6A1` → `61`) nối trực tiếp, vẫn unique và không vượt `app_user.user_name`.
- Bổ sung 4 phòng chức năng, scorebook Toán 6A1 HK1 với 4 cột và 40 score rows; trạng thái non-SCORED giữ `scoreValue = null` theo constraint hiện tại.
- Sửa V27 để Flyway H2 chạy được bằng cách tách từng `ADD COLUMN` và `ADD CONSTRAINT`; không thay đổi semantics production.
- Điều chỉnh score demo về range hợp lệ `5–10`, đồng bộ username compact và notification receipts theo giới hạn `app_user.user_name` 20 ký tự.
- Không seed timetable entries (Plan 081 ghi rõ người dùng tự xếp), lesson log phụ thuộc timetable, score-import session chưa có bảng/contract, teacher-load policy `TBD`, transcript/average/retake và attendance mở rộng.
- Seed slice Plan 081 đã có implementation và focused validation PASS; full backend vẫn cần xử lý riêng OOM/context và PMD baseline ngoài seed scope.
