# Plan 074.5 — Làm rõ giải thích xếp lớp, bỏ nút xếp thủ công và hiệu chỉnh điểm seed

## Trạng thái và version

- Application-document version: `v3` (tiếp nối Plan 074/074.4).
- Status: `IMPLEMENTED — focused checks pass; full repository gates retain baseline failures`.
- Approval evidence: người dùng trực tiếp đồng ý bốn thay đổi ngày `2026-09-23`.
- Giữ nguyên các thay đổi hiện có trong worktree trước Plan này.

## 1. Mục tiêu

1. Với kết quả tự động của profile `REGULAR`, thay giải thích kỹ thuật bằng: `Phân bổ cân bằng theo điểm học tập và tỷ lệ nam, nữ.`
2. Với issue `SCORE_TIE` tại ranh giới chỉ tiêu, hiển thị explanation: `Có nhiều học sinh bằng điểm tại ranh giới chỉ tiêu; giáo vụ cần xếp lớp thủ công.`
3. Hiệu chỉnh seed lịch sử của `STU2600048` để điểm tổng kết năm học `2025-2026` được tính đúng bằng `8.2`, bằng dữ liệu điểm các môn phù hợp; giữ nguyên các học sinh seed khác và để transcript được tính qua service hiện có.
4. Xóa nút `Tiếp tục xếp thủ công` khỏi dialog chi tiết kết quả `MANUAL_REQUIRED`.

## 2. Phạm vi

| Lane | Phạm vi |
|---|---|
| DEV FE | Cập nhật assertion contract trong `placementApi.spec.ts` nếu cần; xóa button và event `continue-manual` không còn dùng trong `PlacementResultDetailDialog.vue`, spec và parent view. |
| DEV BE | Cập nhật explanation trong `PlacementAllocationEngine`; điều chỉnh `DemoHistoricalAcademicSeeder` theo mã học sinh/mã môn để seed điểm năm đúng `8.2`; không thay đổi API shape, eligibility, thuật toán phân bổ hoặc dữ liệu học sinh khác. |
| TEST độc lập | Bổ sung FE assertions cho copy/button và BE regression test cho `SCORE_TIE`, explanation profile `REGULAR`, điểm transcript năm của STU2600048 và tính idempotent. Người test không phải DEV. |
| Coordinator | Review diff, chạy/thu thập validation, cập nhật Dev Notes và summary theo project workflow. |

Không ghi trực tiếp vào database runtime. Seeder vẫn chạy sau Flyway khi cờ demo hiện có được bật; phiên placement đã lưu snapshot không được sửa ngược. Tạo phiên mới sau khi seed nếu cần xác nhận điểm trong placement.

## 3. Kế hoạch kiểm thử backend trước triển khai

- `PlacementAllocationEngineTest`: xác nhận `SCORE_TIE` giữ status/code/severity và trả đúng câu copy mới; xác nhận auto-assignment `REGULAR` dùng câu giải thích mới, profile khác không đổi semantics.
- `DemoDataSeederIntegrationTest.seedsHistoricalGradeSixAcademicRecordForUnassignedGradeSevenStudents`: sau khi seed, tìm student theo code `STU2600048`, transcript `2025-2026` ở trạng thái `FINISH`, assert `finalDtbcn == 8.2` và các subject annual results đủ/đúng; seed lần hai không nhân bản rows và vẫn giữ kết quả `8.2`.
- Cô lập bằng H2/integration context hiện có và `runAllDemoSeeders()`; không truy vấn hoặc cập nhật DB ngoài test.
- Chạy test tập trung, quality checks backend theo `backend-validation`; xem JaCoCo report hiện có để đánh giá dòng/nhánh mới, không đặt coverage threshold mới.

## 4. FE validation

- Dedicated spec cho `PlacementResultDetailDialog` xác nhận không còn nút CTA và không phát event `continue-manual`; thông tin explanation/manual result vẫn hiển thị.
- Placement API/spec fixture xác nhận câu giải thích mới được giữ nguyên; regression cho labels/event còn lại.
- Chạy focused Vitest, FE lint/build và `git diff --check`; báo riêng baseline lint warning nếu có.

## 5. Acceptance criteria

- Explanation thực tế phát ra cho profile `REGULAR` đúng nguyên văn câu người dùng chọn; status/API fields khác không đổi.
- Issue `SCORE_TIE` phát ra đúng nguyên văn câu mới, vẫn là `MANUAL_REQUIRED`/`WARNING`.
- Sau khi seeder chạy, `STU2600048` có `finalDtbcn = 8.2` trong transcript năm `2025-2026`; điểm môn được lưu qua scorebook và tính qua transcript recalculation hiện có.
- Dialog chi tiết không còn nút `Tiếp tục xếp thủ công`; dialog vẫn đóng bình thường và hiển thị lý do/kết quả.
- Không có direct live DB update; không làm mất hoặc ghi đè thay đổi có sẵn ngoài các file liên quan.

## 6. Ghi nhận implementation

- PlacementAllocationEngine dùng đúng hai explanation đã duyệt; status/code/severity và thuật toán không đổi.
- Seeder seed điểm môn STU2600048 theo subject code; mười điểm năm môn là 8.0, 8.1, 8.1, 8.2, 8.2, 8.2, 8.3, 8.3, 8.3, 8.3, tổng 82.0; các điểm thành phần đi qua transcript recalculation hiện có.
- Dialog không còn CTA Tiếp tục xếp thủ công hoặc event/handler điều hướng chỉ phục vụ CTA.
- Focused FE specs PASS 10/10; full FE tests PASS 113 files / 608 tests; coverage và build PASS. FE lint FAIL do hai warning baseline trong NotificationComposer.spec.ts.
- Focused BE PlacementServiceTest PASS 15/15, PlacementAllocationEngineTest PASS, và integration method cho lịch sử/seed PASS với STU2600048 finalDtbcn = 8.2, 10 annual results và score/result idempotency.
- Backend Validation Result: test FAIL (full run 560 tests, 101 failures, gồm 2 seed-fixture assertions và Spring context failures bắt nguồn từ thiếu role ACADEMIC_OFFICE tại DemoIdentitySeeder.java:288); Checkstyle PASS (978 warnings hiện hữu); PMD FAIL (7 vi phạm hiện hữu ở DemoIdentitySeeder); build FAIL tại PMD với cùng 7 vi phạm hiện hữu.
- Không ghi DB runtime; không chạy browser/live. Chi tiết validation và files thực tế nằm trong hai Dev Notes 074.5.
